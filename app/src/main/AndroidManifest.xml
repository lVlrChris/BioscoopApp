<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.groep2.bioscoopapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".guilayer.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".guilayer.DetailActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".guilayer.CinemaActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".guilayer.OrderActivity"
            android:screenOrientation="portrait"
            android:noHistory="true" />
        <activity android:name=".guilayer.PaymentActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".guilayer.UserActivity"
            android:screenOrientation="portrait"/>
    </application>

</manifest>